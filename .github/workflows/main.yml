name: docker-compose-actions-workflow
on:
  push:
    branches:
      - Deployment
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: cd src && docker-compose --env-file .env up
